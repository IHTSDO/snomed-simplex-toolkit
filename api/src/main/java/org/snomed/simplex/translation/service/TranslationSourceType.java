package org.snomed.simplex.translation.service;

/**
 * Identifies translation snapshot sources. Only {@link #TERMINOLOGY_SERVER} is persisted for sync.
 */
public enum TranslationSourceType {
	TERMINOLOGY_SERVER
}
