import { ValidationResultsComponent } from './validation-results.component'

describe('ValidationResultsComponent', () => {
  it('should mount', () => {
    cy.mount(ValidationResultsComponent)
  })
})