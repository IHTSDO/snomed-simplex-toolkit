## Story: <Title>

**As** <role>, **I want** <goal> **so that** <reason>.

### Prerequisites
- Must be logged in as: admin / author / viewer
- Required existing data: (describe any needed state, e.g. "a code system named TEST must already exist")

### Steps
1. <UI action, e.g. "Click the 'New Code System' button">
2. <UI action, e.g. "Fill in the Short Name field with 'TEST'">
3. <UI action, e.g. "Click Submit">
4. ...

### Expected outcomes
- <assertion, e.g. "The new code system appears in the list with name 'TEST'">
- <assertion, e.g. "A success notification is shown">

### Notes
- (Edge cases, known limitations, or hints about which component/route is involved)
- (e.g. "The form is in the CreateCodesystemComponent at /codesystems/new")
